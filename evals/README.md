# 固定回归评测

v2 自带 8 个确定性 fixture：

1. 角色知识泄露
2. 关系无证据突变
3. 旅行时间不足
4. 已耗尽道具再次消耗
5. 使用未拥有能力
6. 时间线倒退
7. 有限视角读取他人内心
8. 伏笔到期未处理

运行：

```bash
cd mcp
uv run novel-production evals
```

或通过 MCP 调用 `novel_run_evals`。这些评测不调用外部模型，因此适合每次修改后快速回归。
